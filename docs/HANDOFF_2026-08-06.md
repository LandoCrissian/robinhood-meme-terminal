# RMT engineering handoff — 2026-08-06

**Status: SUPERSEDED as roadmap — historical handoff only.** Current product direction is defined by [`ARCHITECTURE_FREEZE.md`](ARCHITECTURE_FREEZE.md). Any V7, profile, creator or community roadmap language below is no longer current.

This was the operating handoff for Robinhood Meme Terminal on 2026-08-06. It separates the behavior and work known at that historical point.

## Canonical production baseline

- Production repository baseline: `origin/main` at `71e081d` (`feat(web): harden trade execution and recovery`).
- Public product: `https://www.rmtlaunch.fun`.
- Official V6 RMT market: live and discoverable.
- New V6 token creation: intentionally paused. The public `/launch` route is a fail-closed V7 preparation page.
- External trading: non-custodial Sushi and Uniswap preparation remains live where an independently verified route is available.
- V7 creator publishing, marketplace execution, and new launch contracts: not deployed.
- Live Position Guard automation: not deployed and `NEXT_PUBLIC_RMT_LIVE_POSITION_GUARD_ENABLED=false` must remain the default.

## UI hardening prepared in `codex/handoff-current`

The handoff branch is based directly on current production rather than the conflicted terminal V12 preview branch.

- Initial market loading now says `SYNCING MARKETS`, uses placeholder counts, and shows a real validation state instead of truthful-looking zero markets or zero signals.
- Both independent feed implementations (`external-market-feed.tsx` and `external-market-feed-v10.tsx`) have the same loading behavior.
- Mobile discovery tabs fit within 390px without horizontal page overflow.
- Venue, route, and source filters wrap into a short thumb-reachable layout instead of clipping the source selector.
- Watchlist notification, clear-history, and dismiss controls meet a practical mobile interaction floor.
- The vertical RMT Live launcher and close control have larger thumb targets.
- Disabled mobile execution actions remain in document flow and no longer cover pre-sign evidence.
- Long execution-check labels wrap instead of colliding with adjacent data.
- Price-impact settings are explicitly advisory alerts. They do not masquerade as transaction-integrity vetoes.
- Quote freshness, protected output, route identity, exact simulation, balance, and wallet authorization remain hard execution requirements.
- Public risk copy and V6 release documents now agree that the existing V6 market is live while new V6 creation is paused.

## UI detective verification completed

### Mobile

- Tested at 390 × 844.
- Root page has no document-level horizontal overflow.
- Four discovery tabs fit inside the viewport.
- Route and source controls remain visible without a sideways page scroll.
- Add-funds and trade sheets remain viewport-bound and internally scrollable.
- Profile identity is collapsed after save and can be reopened for editing.
- Admin workspace exposes working exits to Terminal and Profile.
- Watchlist, Explore, Protection, Profile, Status, and Support were inspected.

### Desktop

- Tested at 1440 × 1000.
- Market table and filters fit the viewport without horizontal page overflow.
- Market execution rail remains sticky and scrollable within the viewport.
- Buy and Sell actions remain readable and do not overlap the wallet control.
- Market chart exposes Live, 5m, 15m, 1h, 6h, 24h, and 7d views when data is available.

### Automated verification

- TypeScript type checking: pass.
- Trade ticket smoke suite: pass.
- Public discovery smoke suite: pass.
- Terminal workstation smoke suite: pass.
- Route batch alignment: pass.
- Trade execution reliability: pass.
- Trade diagnostic redaction: pass.
- Trade speed smoke: pass.
- Optimized Next.js production build after the final dual-feed alignment: pass.

## Sushi onchain deadline guard

The separate branch `codex/sushi-deadline-guard` preserves an unfinished but substantive safety upgrade. It must not be merged into production before its contract is deployed and its address/runtime hashes are configured, because the web integration intentionally fails closed when the guard is unavailable.

Prepared work includes:

- `RMTSushiDeadlineGuard.sol`;
- deterministic deployment script;
- unit, adversarial, and Robinhood Chain fork tests;
- server-side runtime hash verification;
- guarded executable-quote construction;
- approval spender migration from RedSnwapper to the guard;
- UI and integration disclosure updates.

Verified on 2026-08-06:

- unit/adversarial contract suite: 12 passed, 0 failed;
- fork boundary suite: 1 passed, 0 failed.

Remaining release gates:

1. Rebase/port the branch onto the production baseline.
2. Run the web smoke/build suite against the rebased implementation.
3. Review deployment bytecode and constructor inputs.
4. Obtain explicit authorization for the irreversible mainnet deployment and gas expenditure.
5. Deploy and verify the guard on Robinhood Chain.
6. Configure the exact address and runtime hashes in the deployment environment.
7. Prove buy, sell, expiry, replay, allowance-clearance, and failure recovery with small-value mainnet transactions.
8. Only then enable and publish guarded Sushi execution.

## Pull request reconciliation

- PR #310 contains useful trader-control work but is draft, conflicted, and has a failed latest Vercel deployment.
- The useful trader-control behavior was manually ported onto current production in `codex/handoff-current`.
- Do not merge PR #310 as-is. After the replacement branch is published and verified, close it as superseded.
- The deadline-guard branch is a separate deployment track and must not be bundled into the UI release.

## Next engineering sequence

1. Review `git diff` and confirm no production secrets or generated artifacts are present.
2. With explicit authorization: stage, commit, push, and open a preview PR for the UI hardening branch.
3. Validate the Vercel preview on mobile and desktop, including a connected external wallet.
4. With separate authorization: merge and release the UI branch.
5. Close PR #310 as superseded only after the replacement is live.
6. Rebase and finish the Sushi deadline-guard branch without changing production routing.
7. Keep Position Guard and V7 contracts disabled until their individual deployment, verification, and monitoring gates are complete.

## Exact prompt for the next Codex conversation

> Continue RMT from `docs/HANDOFF_2026-08-06.md`. First verify the current branch, production SHA, open PRs, and deployment health. Preserve the separate `codex/sushi-deadline-guard` work. Do not merge or deploy contracts without explicit authorization. Finish the current UI hardening preview, validate mobile and desktop end to end, then continue the Sushi deadline-guard release track without interrupting live Sushi or Uniswap trading.
