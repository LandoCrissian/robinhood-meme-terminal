name: CCFF00 TBA read-only probe

on:
  push:
    branches:
      - research/ccff00-tba-probe
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
      - name: Install probe-only viem dependency
        run: npm install --no-save --ignore-scripts viem@2.55.5
      - name: Resolve complete CCFF00 token-bound-account snapshot
        run: node scripts/ccff00-tba-probe.mjs
      - name: Archive read-only snapshot
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ccff00-tba-snapshot
          path: |
            tmp/ccff00-tba-snapshot.json
            tmp/ccff00-tba-snapshot.csv
            tmp/ccff00-tba-summary.json
          if-no-files-found: warn
          retention-days: 7
